import React from "react";

const TextMenu = ({ name }) => {
  return (
    <div className="p-1">
      <p className="text-black text-lg font-semibold">{name}</p>
    </div>
  );
};

export default TextMenu;
